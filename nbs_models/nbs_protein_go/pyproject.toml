[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nbs-protein-go"
version = "0.2.0"
description = "Neighborhood-BoxSquare protein-GO model with BoxSquaredEL geometry"
requires-python = ">=3.10"
dependencies = [
  "torch>=2.4",
  "torch-geometric>=2.5",
]

[tool.setuptools.packages.find]
include = ["nbs_pg*"]
